#include <bits/stdc++.h>
#include <vector>
using namespace std;

class Solution {
    // unfinished.
public:
    int longestSubstring(string s, int k) {
        unordered_map<char, int> freq;
        for(char c: s){
            freq[c]++;
        }
        int maxLength = 0;
    }
};